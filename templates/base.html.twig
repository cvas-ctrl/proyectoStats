<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Rick & Morty Stats{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('css/main.css') }}">
    {% endblock %}
</head>
<body class="bg-light">

<nav class="navbar navbar-expand-lg navbar-dark shadow-sm mb-4 py-2" style="border-bottom: 2px solid #97ce4c; background: rgba(10, 10, 10, 0.95);">
    <div class="container">

        <a class="navbar-brand mx-lg-auto d-flex align-items-center order-0" href="{{ path('app_home') }}">
            <img src="{{ asset('images/logo_stats.png') }}"
                 alt="Logo"
                 style="width: 180px; height: auto; filter: drop-shadow(0 0 5px rgba(151, 206, 76, 0.4));"
                 class="img-fluid hover-zoom">
        </a>

        <button class="navbar-toggler order-2 border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" style="color: #97ce4c;">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse w-100 order-3" id="navbarNav">

            <ul class="navbar-nav ms-auto align-items-center gap-3">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle text-white border border-info border-opacity-25 rounded-pill px-4"
                           href="#"
                           role="button"
                           data-bs-toggle="dropdown"
                           style="background: rgba(0, 176, 200, 0.1); font-family: 'Arial Black', sans-serif; font-size: 0.85rem;">
                            <span style="color: #97ce4c;">ID:</span> {{ app.user.username|upper }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow-lg border-info animate slideIn" style="background: #1a1a1a;">
                            <li><a class="dropdown-item py-2" href="{{ path('admin_site') }}">⚙️ PANEL ADMIN</a></li>
                            <li><hr class="dropdown-divider bg-info"></li>
                            <li><a class="dropdown-item text-danger fw-bold py-2" href="{{ path('app_logout') }}">CERRAR SESIÓN</a></li>
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link text-white fw-semibold small" href="{{ path('app_login') }}">LOGIN</a>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-sm px-4 fw-bold shadow-sm rounded-pill"
                           href="{{ path('app_register') }}"
                           style="background-color: #97ce4c; color: #204529; border: none;">
                            REGISTRO
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

{% block body %}{% endblock %}

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
{% block javascripts %}{% endblock %}
</body>
</html>
